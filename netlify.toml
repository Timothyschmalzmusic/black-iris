[build]
  command = "echo 'Build complete'"
  publish = "."

[functions]
  directory = "functions"

[[headers]]
  for = "/*.html"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"
